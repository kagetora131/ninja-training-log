# CLAUDE.md（忍者修行ログ）

> Claude Code に向けたプロジェクト概要・実装指示書です。

> **注意**：このSupabaseプロジェクト(`rtecihjilnuhhgtpogdw`)には、本アプリ
> (`training_logs`/`user_progress`)とは無関係の`public.ninja_shindan_diagnosis_events`
> テーブルが間借りしている(別リポジトリ「忍者診断アプリ(NinjaJobs)」の匿名
> 結果集計用、個人情報なし)。新規にSupabaseプロジェクトを作ると無料枠の
> activeプロジェクト数上限に近づくため、既存の空き枠を間借りする形にした
> (2026年9月、ユーザー承認済み)。本アプリのマイグレーション・スキーマ変更を
> 行う際は、このテーブルには触れないこと。詳細は忍者診断アプリ側のCLAUDE.md
> 13章を参照。

## 作りたいもの

ログイン機能つきの**習慣化トラッキングアプリ**。日々の運動・学習などの記録をつけると経験値（XP）が貯まり、忍者ランクが上がっていく。既存の「忍者クイズ」「忍者タイプ診断」と世界観を共有するポートフォリオ第3作目。

## 解決したい課題

- 誰が：継続的な習慣化（運動・学習・読書など）が苦手な個人ユーザー
- どんな場面で：日々の記録をただのチェックリストではなく、ゲーム的な達成感とともに続けたい場面
- 何に困っているか：単調な記録アプリは三日坊主になりやすい。ゲーミフィケーション（Duolingo的なランク・経験値システム）で継続のモチベーションを作りたい
- ポートフォリオとしての狙い：認証・DB設計・ユーザーごとのデータ永続化・進捗グラフ表示という「継続利用型Webサービス」に必要な要素をひと通り実装し、単発利用の診断/クイズアプリとの差別化を図る

## 使いたい技術（無料・ライセンスクリア前提）

- フロントエンド：React（MITライセンス）
- 認証：Supabase Auth（無料枠あり、メール認証 or Googleログイン）
- データベース：Supabase（Postgres、無料枠あり、OSSコア）
- グラフ表示：Recharts（MITライセンス）
- アバター/キャラ表現：既存の忍者テーマのイラスト資産を流用、または DiceBear（MITライセンス）で追加パーツ生成
- ホスティング：Vercel 無料枠 または GitHub Pages

## 入力

- ユーザー登録・ログイン情報
- 日々の記録（種類：運動 / 学習 / 読書 など、時間または回数、任意メモ）

## 出力

- 記録に応じたXP加算とランクアップ演出（例：下忍 → 中忍 → 上忍）
- 週次・月次の進捗グラフ（Rechartsで可視化）
- 連続記録日数（ストリーク）表示
- （将来拡張）デイリー/ウィークリーのランキング表示 → 既存の忍者クイズにも展開可能な設計にしておく

## 忍者ランク設計（確定・2026-09-04更新）

| ランク | 必要累計XP | 称号イメージ |
|---|---|---|
| 1 | 0 | 見習い忍者 |
| 2 | 100 | 下忍 |
| 3 | 300 | 中忍 |
| 4 | 700 | 上忍（最高ランク） |

当初案では上に「5: 1500XP 忍者頭領」があったが、実際の忍術修行における各種資格・段位を
踏まえ「頭領」を名乗るのは大仰すぎるとの判断で廃止し、上忍を最高ランクとした。

XP付与ルールは当初「記録1件＝固定XP」で開始したが、カテゴリ別の固定XP（稽古15/学習20/
読書10/瞑想15/その他10）に変更済み。連続記録ボーナス等はさらなる将来拡張として残す。

## データモデル（初期案）

```
users
  id, email, display_name, created_at

training_logs
  id, user_id, category(運動/学習/読書...), amount, memo, logged_at

user_progress
  user_id, total_xp, current_rank, current_streak, longest_streak, last_logged_date
```

## 実装の進め方（MVP方針）

1. Supabase Authでのユーザー登録・ログインを実装
2. `training_logs` へのCRUD（記録の追加・一覧表示）を実装
3. XP計算ロジックとランク判定を実装し、記録後にランクアップ演出を出す
4. 週次・月次グラフをRechartsで実装
5. （後回しでOK）ストリーク計算、デイリーランキング機能

## 未確定事項（要確認しながら進める）

- ランクごとの必要XPやXP付与ルールの最終バランス調整
- ソーシャルログイン（Google等）を導入するか、メール認証のみにするか
- 忍者クイズとのデータ連携（将来的に共通ユーザーアカウントにするか）は現時点では別プロジェクトとして独立させる
